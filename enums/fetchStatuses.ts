// eslint-disable-next-line @typescript-eslint/naming-convention
enum FETCH_STATUSES {
  LOADING = 'loading',
  SUCCESS = 'success',
  ERROR = 'error',
  IDLE = 'idle',
}

export default FETCH_STATUSES;
