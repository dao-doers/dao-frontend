/* eslint-disable import/prefer-default-export */
import styled from '@emotion/styled';

export const TooltipMessageText = styled.span`
  text-align: left;
  font-size: 16px;
  letter-spacing: 0;
  opacity: 1;
  font-weight: 400;
`;
